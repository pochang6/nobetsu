# これからやること

思いついた順ではなく、**使っていて実際に困った順**に並べています。
片付いたら消して、README の「これから」と食い違わないようにしてください。

## 次に手を付けたいもの

- [ ] **開始キーを設定から変えられるようにする**
      いまは ⌘ の長押し固定（右⌘ だけに絞る逃げ道はある）。
      メニューから「開始キー」を選べるようにしたい。
      キーを押して覚えさせる形（記録ボタン）が分かりやすい。
      `TriggerMonitor` は keyCode 直値を持っているので、そこを設定値にする。
- [ ] **辞書に前後の条件を持たせる**
      いまは単純な文字列置換なので、「苦労」のような普通の言葉は
      長めに取って避けるしかない。行頭・行末や、直後の文字を条件にできると安全。
      正規表現まで入れるかは、実際に困ってから決める。
- [ ] **辞書をメニューから足せるようにする**
      「いま入った文字を選んで、正しい表記を登録」まで行けると、
      ファイルを開かずに育てられる。

## いずれ

- **音声入力中だと、入力欄そのものを見て分かるようにしたい。**
  文字カーソル（キャレット）の色を変えられれば一番いいが、
  **キャレットを描いているのは相手のアプリなので、こちらからは変えられない。**
  代わりにできそうなのは、入力先のウインドウの縁を薄く光らせる、
  画面の縁に細い線を出すなど。目印より視線の近くに出せるのが利点。

- [ ] 誤変換の抑制（`SFSpeechLanguageModel` によるカスタム言語モデル）。
      辞書は対症療法で、こちらが本命。ただし学習データの用意が要る
- [ ] App Intents 対応（Shortcuts.app から呼べるようにする）
- [ ] 挿入先ごとの挙動差の検証（ターミナル / ブラウザ / Electron）
- [ ] 目印の位置を「入力欄の近く」に出す案（いまは画面の左上に固定）

## 分かっていて、直していないこと

- **同じアプリの中で入力欄から離れた判定は、アプリによっては効かない。**
  通知ではなく 0.4 秒ごとに見に行く方式にしたので大半は拾えるが、
  入力欄の場所を最後まで教えてくれないアプリでは判断できない。
  アプリの切り替えと画面（Space）の切り替えは、どのアプリでも確実に拾える。
- **送信（Enter）の直後は、区間の切れ目が届くまで一瞬だけ間が空く。**
  喋り続けていれば拾い直すようにしたが、間そのものは消えていない。
- **フォーカスを失っても、元の入力欄へ書き込み続けられる見込みがある。**
  診断ログに `focus: 入力欄が見えた (AXTextArea / 値=true 選択文字=true)` が出た。
  `選択文字=true` は、**フォーカスを持たないまま Accessibility 経由で
  その入力欄へ文字を挿し込める**という意味。
  キーイベントは必ず前面のアプリへ届くので迂回できないが、この口を使えば
  「よそを触っていても、喋った文字は元の入力欄に入り続ける」が作れる。
  ただし開いているかどうかはアプリしだいなので、
  使えないアプリでは今の挙動（止まる）へ落とす作りが要る。
- **入力欄を教えてくれないアプリがある。**
  Claude や Slack のような Electron 製アプリは、頼まれるまで
  `AXFocusedUIElement` を返さない。見張りはじめに `AXManualAccessibility` を
  立てて用意してもらっている（支援技術と同じ頼み方）。
  それでも見えないアプリでは、アプリと画面の切り替えだけで守る。
  立てたままにしているので、相手のアプリが重くなるようなら考え直す。
- **辞書から規則を消したときは、`./build.sh` をしないと消えたことにならない。**
  アプリに焼き込まれた同梱辞書に古い規則が残り、そちらが生き続ける。
  追加は新しい方が勝つので、そのまま効く。
- **辞書は未確定テキストにも当たる。**
  認識が途中の一瞬だけ変な形に化けることがあるが、次の更新で打ち直される。

## 2台目の Mac で使うとき

会社の Mac でも同じものを使う場合、辞書の置き場所で挙動が変わります。

| 置き場所 | 共有されるか | 反映のしかた |
|---|---|---|
| `dictionary.txt`（リポジトリ） | される（git） | `./build.sh` |
| `~/Library/Application Support/nobetsu/dictionary.txt` | されない | 保存するだけ |

両方の Mac で効かせたい言い換えは、リポジトリ側に書いて push してください。
片方の Mac だけの事情（社内用語など）は個人辞書へ。
