# nobetsu — 開発の手引き

日本語話者向けの macOS 音声入力アプリ。⌘ を長押しして喋ると、話した端から、
そのとき使っているアプリに文字が入る。IME を経由せず、時間制限もない。

このファイルは AI エージェントと人間の両方に向けた作業メモです。
個人環境に依存する値は書かないでください（絶対パス、資格情報、個人名など）。

## 環境

- macOS 26 (Tahoe) 以降 / Apple Silicon
- Swift はコマンドラインツール同梱のもので足りる。**Xcode は不要**
- 依存ライブラリなし。すべて OS 標準のフレームワーク

## ビルドと動作確認

```bash
./build.sh --check              # 署名できるか（ビルドはしない）
./build.sh                      # ビルドして /Applications/nobetsu.app に設置
pkill -f nobetsu.app            # 動いているものを終了
open /Applications/nobetsu.app  # 起動
```

**変更したら必ずこの3つを通してください。** `/Applications` に置くのは、
システム設定の許可一覧（入力監視・アクセシビリティ）が
そこからしかアプリを選べないためです。

証明書が無い環境では `./build.sh` は**設置せずに止まります**（下の「署名」）。

起動してもウィンドウは出ません。メニューバーの波形アイコンだけが目印です。

| アイコン | 状態 |
|---|---|
| 警告の三角 | 許可が足りていない |
| 波形 | 待機中 |
| 波形＋丸 | 認識中 |

## テスト

```bash
./test.sh
```

副作用を持たない部分だけを確かめます（辞書の置換、打ち込みの差分計算、許可が無いときの案内、開始キーの設定、ログの圧縮）。
アプリは起動しません。`Sources/App.swift` を含めないのは `@main` が衝突するためです。

**この5つを選んだ理由**は、間違えると利用者を壊すか、壊れても気づけないからです。
`Injector` の差分計算を誤ると Backspace が他人の文章を削りにいきますし、
辞書の置換が冪等でないと、打ち直すたびに文章が壊れていきます。
`PermissionAdvice` の文言を誤ると、利用者は**動かない理由にたどり着けないまま終わります**。
TCC の返事そのものは実機でしか見られませんが、**返事を受けて何を出すか**はただの組み立てなので、
そこだけ切り出して確かめています。
`Log.gzipped` は少し毛色が違って、**壊れていても普段は誰も気づきません**。
気づくのは半年後、古い記録を開こうとして開けなかったときです。

新しく純粋な処理を書いたら、ここに足してください。
逆に、マイク・イベントタップ・他アプリへの打ち込みは実機でしか確かめられません。
そちらは `.claude/skills/rebuild/` の手順で動かして見ます。

## ログ

```bash
tail -30 ~/Library/Logs/nobetsu.log
```

**不具合を調べるときは、推測で直す前に必ずログを読んでください。**
このプロジェクトでは、ログを入れずに推測で修正を重ねて半日を失いました。
ログを入れた直後に原因が一発で判明しています。

起動、許可の状態、署名の種類、トリガー、エンジンの遷移、音の再生、
マイクの解放が記録されます。

**喋った内容そのものは書きません。** 残るのは時刻・アプリ名・文字数だけです
（`injector: 送信（打ち込み済み 52 文字）`）。
打ち込んだ文字を残せば調査は楽になりますが、そうすると**このログは
「利用者が一日じゅう喋った内容の写し」**になります。ログは平文で、
Time Machine にも入り、不具合の報告と一緒に人へ渡ることもあります。
新しくログを足すときも、認識テキストは渡さないでください。

**1MB を超えたら圧縮して退避します。**いまのファイルを含めて5世代です。

```
nobetsu.log        いま書いているもの（1MB まで）
nobetsu.log.1.gz   直前のもの
…
nobetsu.log.4.gz   いちばん古いもの。次の退避で捨てられる
```

追記しかしないので、放っておけば際限なく育ちます（実測で1日 40KB ほど、
かなり喋った日で）。捨てるのではなく圧縮して残すのは、
**不具合の多くは「前はこうではなかった」の形で来る**からです。
gzip は約 1/9 に縮むので、全部合わせても 1.5MB を超えません。

読むときは `gunzip -c` を使ってください（macOS の `zcat` は `.Z` を探しにいきます）。

```bash
gunzip -c ~/Library/Logs/nobetsu.log.1.gz | grep focus:
```

退避したあとのファイルの1行目には行き先を書きます
（`──────── ここより前は nobetsu.log.1.gz ────────`）。
これが無いと、読んでいる人には「前半が消えた」としか見えません。

圧縮は OS の `Compression` に任せていますが、`COMPRESSION_ZLIB` が返すのは
**ヘッダの無い生の DEFLATE** なので、gzip のヘッダ・CRC32・元の長さは
`Log.gzipped` で自分で付けています。ここは入力と出力だけで決まるので
`./test.sh` で確かめています（**圧縮が壊れていても普段は誰も気づかず、
気づくのは半年後、古い記録を開こうとしたときになる**ため）。

## 署名（ソースからビルドするなら必須）

macOS の許可は**署名の同一性**に紐づきます。アドホック署名にはそれが無いため、
ビルドのたびに別アプリ扱いになり、許可をやり直すことになります。
入力監視にいたっては、尋ねられることすらなく拒否されます。

キーチェーンアクセスで **`nobetsu`** という名前のコード署名用の自己署名証明書
（固有名のタイプ「自己署名ルート」）を作ってください。`build.sh` が自動で使います。
作ったあと、証明書の「信頼」を開いて**コード署名を「常に信頼」**にする必要があります。
初回のビルド時にキーチェーンのパスワードを聞かれたら「常に許可」を選んでください。
手順は README の「ソースからビルドする前に」にもあります（利用者向けの文面はそちらが正）。

**「ビルドできる」と「使える状態で置ける」は別のことです。**
証明書が無くてもコンパイルは通り、アドホック署名のアプリはできあがります。
ところがそれは**⌘ の長押しに一切反応しない置き物**で、しかも利用者からは
理由が一切見えません（許可のダイアログすら出ないため、押し忘れとも区別がつかない）。
実際、別の Mac へ移したときにこれで丸一日溶かしました。
だから `build.sh` は**設置する前に止まります**。動かないものを `/Applications` に残しません。

**署名できるかどうかは、実際に署名して判定します。**
`security find-identity -v -p codesigning` が 0 件を返すのに
`codesign --sign nobetsu` は通る、という食い違いを実際に見ました（信頼設定の差）。
一覧に出るかではなく、署名できるかが知りたいことなので、一覧は当てにしません。
`build.sh` は temp に置いた Mach-O へ試し打ちしてから進みます。

コンパイルが通ることだけ確かめたいときは、意図を明示して先へ進められます。

```bash
NOBETSU_ALLOW_ADHOC=1 ./build.sh   # アドホックで設置する。許可は下りない
```

**証明書が要らないのは、署名・公証された .app を配布で受け取った場合だけです。**
その場合でも**入力監視とアクセシビリティの許可は要ります**（別の話です）。
いまは配布版がないので、実質「使う人もソースからビルドする＝証明書が要る」と考えてください。

## 構成

| ファイル | 役割 |
|---|---|
| `Sources/App.swift` | 全体の配線、目印のメニュー、許可の要求 |
| `Sources/StatusMenu.swift` | メニューバーのアイコンとメニュー（AppKit）。チェック項目は押しても閉じない |
| `Sources/Dictation.swift` | `SpeechAnalyzer` + `DictationTranscriber` による認識 |
| `Sources/Injector.swift` | 最前面のアプリへ文字を打ち込む。**ここが核心** |
| `Sources/Trigger.swift` | `CGEventTap` によるキーの見張り |
| `Sources/FocusWatcher.swift` | 入力先から離れたかの見張り。離れたら止める |
| `Sources/Phrases.swift` | 辞書（言い換え表）の読み込みと適用 |
| `Tests/main.swift` | 副作用を持たない部分のテスト。`./test.sh` で走る |
| `Sources/Indicator.swift` | 認識中の目印（フォーカス中の入力欄に追従する小さな表示） |
| `Sources/Overlay.swift` | 認識中の文字を流す表示（既定で無効） |
| `Sources/Permissions.swift` | 入力監視とアクセシビリティの判定と要求 |
| `Sources/PermissionAdvice.swift` | 許可が無いときにメニューへ出す文言。副作用なし・テストあり |
| `Sources/Sounds.swift` | 開始音と終了音 |
| `Sources/LoginItem.swift` | ログイン時の自動起動 |
| `Sources/Log.swift` | ログ出力。1MB を超えたら圧縮して退避する（いまのものを含めて5世代） |
| `dictionary.sample.txt` | 公開する見本。同梱するのはこれだけ |
| `dictionary.txt` | 旧形式の個人辞書。Git では追跡せず、その場所で読み続ける |
| `Sources/DictionaryStorage.swift` | 個人辞書・旧辞書を上書きせず引き継ぎ、設置前に退避する |
| `Sources/DictationLifecycle.swift` | 準備・収録・停止の状態と世代の管理 |
| `update.sh` | 最新コードの取得から起動確認まで |
| `CHANGELOG.md` | 公開する変更履歴。版を上げるコミットで追記する |
| `TODO.md` | これからやること。実際に困った順に並べる |
| `PUBLISH.md` | OSS として公開するまでの計画。**公開前に必ず読むこと** |
| `.claude/skills/dictionary/` | 誤変換を見つけて辞書に育てる手順（AI 向け）。`check.sh` で辞書全体を点検できる |
| `AGENTS.md` / `.agents/skills/` | Codex 向けの写し。内容の正本は `CLAUDE.md` / `.claude/skills/` |
| `.local/` | この Mac だけの覚書。**git では追跡しない**（下の「手元の覚書」） |
| `spike/` | 採用判断のための計測アプリ（本体とは独立） |

## 手元の覚書（`.local/`）

公開リポジトリに出すものではないけれど、**AI エージェントには毎回見えていてほしいもの**を
`.local/` に置いています。`.gitignore` 済みで、クローンした人の手元には存在しません。

| ファイル | 中身 |
|---|---|
| `.local/NOTES.md` | いま何を試しているか。まだ push していないもの。製品へ入れるか決めかねているもの |
| `.local/dictionary-level` | 辞書スキルの段階（1〜4）。1行目の数字がそれ |

**セッションのはじめに `.local/NOTES.md` を読んでください。**
無ければ何もしなくてよく、あればそこが最新の状況です。

置くのは**まだ形になっていないもの**だけです。
落ち着いた話は `TODO.md` と `PUBLISH.md` へ移します。
このリポジトリは「使いながら、よかったものだけ製品へ入れる」進め方をしているので、
**試している最中のものが常にどこかにあります。**その置き場が無いと、
次のセッションのエージェントには「なぜか中途半端なコードがある」としか見えません。

## 設計上の決定（変更する前に読むこと）

**IME を経由しない。**
macOS 標準の音声入力は認識した「よみ」を日本語入力に流し込むため、
ライブ変換が未確定文節を書き換え続けるのと衝突して固まります。
`DictationTranscriber` は漢字かな交じりの確定済みテキストを返すので、
`CGEvent` の Unicode 直接入力を使えば変換工程を丸ごと迂回できます。
**この一点がこのアプリの存在理由です。**

**確定を待たずに打ち込む。**
未確定テキストを先に打ち、認識が書き換わったら共通接頭辞を求めて、
食い違った末尾だけを消して打ち直します。確定を待つと 20 秒以上文字が出ません。

**`SpeechTranscriber` は使わない。**
計測では初回表示に 11.79 秒、確定が最大 8.34 秒遅れました。
喋りながら見る用途には使えません。`DictationTranscriber` を使ってください。

**許可は 入力監視 → アクセシビリティ の順に求める。**
`AXIsProcessTrusted()` を先に呼ぶと入力監視の要求が通らなくなる既知の不具合があります。
また、キーを飲み込めるタップ（`.defaultTap`）は**両方**の許可を要求します。
許可ダイアログを出すのは `CGRequestListenEventAccess` であって、
`CGEventTap` の生成ではありません。生成は黙って `nil` を返すだけです。

**許可が足りないときは、黙って警告を出すのではなく、理由を書く。**
メニューバーの三角だけでは「壊れている」としか読めません。
足りない許可・それが何の許可か（入力監視＝キーを読む／アクセシビリティ＝文字を入れる）・
いま動いているアプリのパス・設定画面への導線を、メニューだけで完結させます。
特に**「許可が無い間は ⌘ を長押ししても何も起きない（そこで聞かれもしない）」**は必ず書きます。
これを知らないと「押せば聞かれるはず」と待ち続けて詰みます。
文言の組み立ては `PermissionAdvice`（副作用なし）にあり、`./test.sh` で確かめています。

**アドホック署名は、許可の話より先に出す。**
その状態では何をどう許可しても入力監視は下りないので、
設定画面の案内を出すのは親切ではなく遠回りです。署名を直す話だけをします。

**開始の長押しは既定1.0秒、0.5〜2.0秒を0.1秒刻みで変えられる。**
以前の0.5秒では、⌘C / ⌘V の次のキーを選ぶまで迷っただけで音声入力が始まりました。
ショートカットの相手キーが来れば開始を取り下げますが、0.5秒以内に必ず来るとは限りません。
既定は安全側へ延ばし、手の速さに合わせて戻せるようにします。
開始に使う⌘は左右・左だけ・右だけから選べますが、**停止の単独タップは左右とも受けます。**
開始側を絞ったせいで、とっさに止められなくなる方が危険だからです。

**打ち込みの停止はイベントタップの中で同期的に行う。**
コールバックはキーがアプリへ届く「前」に呼ばれます。ここで止めないと、
Enter で送信した直後に、溜まっていた文字が空の入力欄へ流れ込みます。
処理は配列の退避とタイマー停止だけなので、タップを塞ぐ心配はありません。
**ただし重い処理（ダイアログ表示、エンジン起動）は必ず非同期に逃がしてください。**
コールバックが長引くと macOS がタップを無効化し、以後キーが一切効かなくなります。

**開始音は「聞ける状態になってから」鳴らす。停止音は「押した瞬間」に鳴らす。**
音が鳴った瞬間に喋りはじめるのが自然なので、鳴らすのが早すぎると頭が削られます。
以前は無線イヤホンの接続切り替え（0.5秒ほど音が途切れる）に飲まれるのを避けるため
マイクを起こす直前に鳴らしていましたが、**先頭の一文字が入らない**という
別の問題を生みました。いまはマイクが動いてから 0.25 秒置いて鳴らします。
止めるときは逆で、押した瞬間に鳴らします。待たせると無反応な時間が生まれます。

**停止したら必ずマイクを手放す。**
`AVAudioEngine` を止めるだけでは入力ノードがマイクを掴んだままになり、
macOS 標準の音声入力が起動直後に切れます。
tap を外す → 停止 → reset → インスタンス破棄まで揃えてください。

**辞書は「打つ直前」に当てる。**
音声認識は「Clone」を「苦労」と書く。話し言葉としては正しいので、
認識器の側では直りません。未確定テキストは毎回その区間の全文が届くので、
`PhraseBook.apply` は状態を持たない純粋な置換で足ります。
途中で一瞬おかしな形に化けても、次の更新で打ち直されます。
公開サンプル → 引き継いだ旧辞書（あれば）→ 個人辞書の順に読み、後者が勝ちます。
個人辞書の内容はアプリへ焼き込まず、ログにも出しません。既存ファイル・リンクを更新で上書き・移動・削除しません。
`DictionaryStorage` が設置前にローカルへ退避します。退避に失敗したら設置を止めます。
退避は完成後に古い世代を削除し、最新5世代までにします。現行の辞書・既存リンクは整理対象にしません。
新しく引き継ぐ個人辞書は作業ツリーからコピーし、作業ツリーへのリンクは作りません。
読み込みはファイルごとに行い、読めた規則を使います。読めない保存先と対処はメニューへ出します。
壊れた移行記録は原文を退避し、上書きや再移行をせず設置を続けます。
消えたリンク先はリンク情報を退避して設置を続けますが、残っている実体の退避失敗では止めます。
読むのは**喋りはじめる瞬間**なので、書き換えたら次のひと言から効きます。

**Enter は「送信」であると同時に「利用者の操作」でもある。**
イベントタップは `userTookOver()` を先に呼ぶので、`userSubmitted()` に届く時点で
`pending` は既に空になっており、打ち込み済みの内容は `abandonedPrefix` にあります。
これを知らずに `pending` を送信済みと見なすと「何も打っていない」ことになり、
**その区間の全文が、空になった入力欄へ丸ごと打ち直されます。**
送信した文章がそのまま次のメッセージに現れる形で実際に起きました。
Enter を2回叩いたときに短い方で上書きしないことも含めて、
`submittedPrefix` は**縮む方向へ更新しません**。

**送信したら、その文章は二度と触らない。**
認識は後から言い回しを直します。送信したあとにその直しが届くと、
食い違った位置まで消して打ち直そうとしますが、そこはもう画面にありません。
結果、**送信し終えた文章の後半が、丸ごと空の入力欄へ打ち込まれます**
（実際に「最後の100文字が残る」形で起きました）。
送信済みの文章はもう相手の手元にあり、こちらから直す手立てはありません。
`virtualPrefix` は「打ち込み済みと見なしているが、実際には画面に無い文字数」で、
そこより手前が書き直されたら、その区間は追いかけるのをやめます。

**Shift+Enter は送信ではない。**
Enter を送信とみなすと区間を切り、切れ目の確定が届くまで打ち込みを止めます。
これを改行（Shift+Enter）でやると、空行を入れて喋りはじめた瞬間に
数秒なにも入らず「固まった」ように見えます。実際にログで再現しました
（`区間を区切る` が 0.17 秒差で2行並ぶ＝空行を入れた Enter 2連打）。
Shift が付いた Enter は改行として扱い、区間を切りません。
無修飾の Enter は送信のままですが、そのあとも喋り続けていれば
`tryResumeAfterSubmit` が拾い直すので、止まったままにはなりません。

**入力先から離れたら止める（既定）。通知ではなく、見に行く。**
始めたことを忘れたまま別のアプリへ移ると、独り言が同僚宛の入力欄へ流れ込みます。
事故の代償が大きいので、既定は「離れたら止める」に倒しています。

最初は Accessibility の「フォーカスが変わった」通知で作りましたが、**逆に動きました。**
離れても止まらず、戻ってきた瞬間に止まる。通知はアプリによって出たり出なかったりし、
前面に戻る一瞬だけ別の要素を指すため、その瞬間を「離れた」と誤解するためです。
いまは 0.4 秒ごとに**今のフォーカスを見に行き**、
「文字を入れられない場所にいる」が2回続いたときだけ止めます。
アプリの切り替えだけは通知の方が速いので併用しています。

**「見えない」は「離れた」ではありません。**
入力欄を一切教えてくれないアプリがあります（Claude や Slack のような Electron 製が典型）。
そこで「フォーカスが取れない＝離れた」と判断したところ、
**喋りはじめて 0.8 秒で必ず止まる**という、使い物にならないものができました。

Electron/Chromium 製のアプリは、頼まれるまで入力欄の情報を作りません。
見張りはじめに `AXManualAccessibility` を立てて用意してもらいます
（支援技術が使うのと同じ頼み方）。それでも見えないアプリでは、
アプリと画面の切り替えだけを見ます。**一度も見えていないことを理由に止めません。**

**入力欄かどうかは、役割で決めます。「選択範囲を持っているか」では決められません。**
Chromium は入れ物（`AXGroup`）にも選択範囲を持たせるため、
文章の上をクリックしただけで「まだ入力欄にいる」と誤判定しました。
`AXWebArea`（ページそのもの）も入力欄ではありません。
ページに focus が乗っているだけで、そこへ打った文字はどこへも入りません。

**最初に見えた場所は、役割を問わず基準にします。**
利用者はいまそこで喋りはじめたのだから、そこが入力先に決まっています。
見慣れない役割名を返すアプリを、いきなり「入力欄ではない」と切り捨てないためです。

**相手のアプリへの問い合わせを、メインスレッドでやってはいけません。**
Accessibility の問い合わせは同期的で、相手が応答しなければその場で待たされます。
メインスレッドは**キーの見張り（イベントタップ）と同じ**なので、
そこが詰まると `⚠️ イベントタップが無効化された (timeout)` が出て、
**Mac 全体のキー入力が止まったように見えます。**
問い合わせは別のキューへ逃がし、結果だけを持ち帰ってください。
目印の 25Hz タイマーを消し忘れないのも同じ理由です。

自分（目印のメニュー・メニューバー）が前面にいる間は判定しません。
設定を見ようとしただけで止まってしまうためです。
同じアプリの中で別の**入力欄**へ移っただけなら止めず、打ち込み済みの位置だけ捨てます。

**戻ってきたら、カーソルの位置を見る。**
よそを触って入力欄へ戻ると、アプリがカーソルを先頭へ置き直すことがあります。
そこへ打ち込むと、書きかけの文章の**頭に**続きが入ります。
「先頭ちょうど・選択なし・中身は空でない」ときだけ末尾へ送ります。
自分でカーソルを置いた場合は触りません。途中に差し込みたいことはあるからです。

**押していないのに止まるときは、音を変える。**
終了音と同じでは、何が起きたのか分からないまま驚くだけになります。
止まった理由はメニューにも残します。

**目印は入力欄のある画面へ追従し、画面内の位置は保つ。**
入力欄の AX 座標を別キューで調べ、表示する画面を選びます。座標が見えない場合は
前面ウィンドウ、次にマウスのある画面を使います。Space 遷移後にも前面表示を更新します。
ドラッグした位置は従来どおり画面の左下からの距離で覚えます。入力欄のすぐ近くへ自動配置すると、
入力欄の移動・スクロール・画面切り替えで目印が飛び回るため、画面選択と画面内の位置を分けます。
入力欄がスクロールバック全体を表す場合に備え、画面選択ではウィンドウとの共通部分を使います。
共通部分が空ならウィンドウを使い、はみ出した入力欄だけで別画面へ飛ばさないこと。
画面の選択自体も `IndicatorPlacement` の純粋関数で検証します。
AX の左上原点と AppKit の左下原点を混ぜないこと。問い合わせ結果には世代を付け、
別の入力先へ移った後に古い座標やフォーカス判定を当てないでください。
表示・非表示のアニメーションにも世代を付け、前の hide の完了で新しい表示を消さないこと。

**目印の画面固定は、メニューから明示的に選ぶ。**
既定は入力欄への自動追従。ドラッグは画面内オフセットだけを変更し、固定先を切り替えません。
固定先は接続順や一時的な画面番号ではなく、ColorSync の表示 UUID を UserDefaults へ保存します。
未接続の間は自動追従へ戻し、設定は保持して再接続時に復帰します。
「入力欄の画面へ自動で移動」で固定解除。「位置を初期状態に戻す」はオフセットだけを消します。

**チェック項目を切り替えてもメニューを閉じない。**
普通の `NSMenuItem` や SwiftUI の `Toggle` は押した瞬間にメニューが閉じるため、
入ったのか外れたのかをもう一度開いて確かめることになります。
macOS の SwiftUI には閉じないようにする手段が無い（`menuActionDismissBehavior(.disabled)` は
macOS では使えない）ので、メニューバーは `MenuBarExtra` ではなく AppKit の `NSMenu` で組み、
チェック項目は `NSMenuItem.view` に載せた自前のビュー（`MenuToggle`）にしています。
ビューの中で起きたクリックはメニューを閉じません（長押し時間のスライダーと同じ仕組み）。
代わりにその行はマウスを載せても反転せず、矢印キーでも選べません。
選ぶと閉じる項目（⌘ の左右、画面の固定先）はそのまま普通の項目です。

**目印はフォーカスを奪ってはいけない。**
奪うと打ち込んだ文字が入力先ではなく目印に入り、機能そのものが壊れます。
`nonactivatingPanel` と `canBecomeKey = false` で担保しています。

## よくある調査

| 症状 | まず見るところ |
|---|---|
| ⌘ 長押しに反応しない | ログの `trigger:` 行。許可の状態が出る |
| 許可のダイアログが出ない | ログの `署名` が `adhoc`。`./build.sh --check` を見る |
| ビルドが設置まで進まない | 証明書が無い。`./build.sh --check` の言い分どおりに直す |
| 文字が入らない | アクセシビリティの許可。ログの `start:` 行 |
| 音が鳴らない | ログの `sound:` 行。無線イヤホンは接続切替で音が途切れる |
| 文字が重複する / 消える | `Injector.swift` の区間管理。`accepting` と `discardCurrentSpan` |
| 送信した文章が入力欄に残る | ログの `injector: 送信済みの部分が書き直された`。`virtualPrefix` |
| 他アプリの音声入力が壊れる | ログの `audio: マイクを解放した` が出ているか |
| 喋っているのに数秒なにも入らない | ログの `engine: 区間を区切る`。送信扱いの Enter が犯人 |
| 勝手に止まる | ログの `focus:` 行。離れたと判断した理由と、相手の役割名が出る |
| キー入力ごと効かなくなる | ログの `イベントタップが無効化された`。メインスレッドが詰まっている |
| 目印の上でカーソルが消える | `Indicator.nudgeCursor`。隠しているのは相手のアプリ |
| 入力欄を離れても止まらない | ログの `focus:` 行の役割名。`AXGroup` を入力欄と誤認していないか |
| 辞書が効かない | ログの `phrases:` 行。読み込んだ件数と読んだファイルが出る |

## 運用

- バージョンは `VERSION` ファイルで管理し、`build.sh` が Info.plist に埋め込みます
- 自分の変更は `main` へ直接コミットします（ブランチも Pull Request も挟みません）
- 外部からの変更はフォークと Pull Request で受け付けます
  （フォークなしに他人がこのリポジトリへブランチを作ることはできません）

### 版の上げ方

**判断の基準は「いま動いている人に何が起きるか」だけです。**
コードを触ったかどうかでは決めません。触っても誰にも何も起きない変更はいくらでもあります。

| | どんな変更か | 使う人がすること |
|---|---|---|
| **メジャー** `X.0.0` | 今までのやり方が通じなくなる | 移行の作業が要る |
| **マイナー** `x.Y.0` | できることが増える。目に見えて変わる | 何もしなくてよい |
| **パッチ** `x.y.Z` | 直しただけ。前と同じように使える | 気づかない |
| **据え置き** | ドキュメント・コメント・テストだけ | — |

このリポジトリでの例:

- **メジャー** … 辞書の書式を変える／開始キーの既定を ⌘ から変える／設定の保存先を変えて
  引き継がない／必要な macOS の版を上げる／署名や Bundle ID を変えて**許可の付け直しが要る**
- **マイナー** … 設定項目が増える／目印に操作が増える／App Intents に対応する／
  認識エンジンの構成を変えて挙動が変わる
- **パッチ** … 打ち込みの取りこぼしを直す／ログを足す／文言を直す／`build.sh` の親切さを上げる
- **据え置き** … README だけ、この CLAUDE.md だけ、`Tests/` だけ

**迷ったら軽い方へ倒します。** 版は宣伝ではなく、
「前と何が違うか」を後から自分が思い出すための札です。
小さい変更で大きく振ると、本当に大きい変更のときに言うことが無くなります。

迷う典型は**壊れていた場所を直したとき**です。
「証明書が無ければ `build.sh` を止める」は、`build.sh` の振る舞いが変わるのでマイナーに見えます。
しかし**それまで正しく動いていた人には何も起きません**（止まるのは元々使えなかった場合だけ）。
だからパッチです。**新しく何かが「できるようになった」のか、
それとも「できるはずのことが、できるようになった」だけなのか**で見分けます。

版を上げるのは中身と同じコミットです。分けると、どの版に何が入ったのか分からなくなります。

**タグは「版を上げたコミット」に打ちます。**`VERSION` を変えたコミットが、その版の実体です。
タグを先に打ってから同じ版のまま作業を続けると、**タグとその版の中身がずれます**
（v1.0.0 が実際にそうなっていて、タグの後に2つコミットが乗りました）。
打ち忘れたら、その版の**最後**のコミットに打ちます。
**一度公開したタグは動かしません。**Release が紐づいているならなおさらです。

## 更新の検証

`./build.sh --build-only` は署名付きアプリを作るだけで、設置と個人辞書へのアクセスはしません。
`./build.sh --restart` は設置後に再起動し、ビルド固有 ID の起動ログでキー監視開始まで確認します。
設置・起動確認の失敗時は旧アプリへ戻します。`./update.sh` は fast-forward 更新の後にこれを呼びます。
`./test.sh` は純粋な処理に加え、一時フォルダ内の辞書引き継ぎと模擬アプリのロールバックも検証します。
実際の個人辞書や `/Applications` はテストで変更しません。

開始処理は準備中を含めて1本だけです。停止はその場で世代を無効にしてマイクを返し、
後始末中は次の開始を受け付けません。古い結果や遅れて届く開始音を次の収録へ流さないでください。
録音中の無音は待機状態ではありません。AirPods の再生操作との競合を調べる場合は、録音停止後と区別します。

AirPods の拒否バナーは未解決です。`AVAudioApplication.setInputMuteStateChangeHandler` の
登録と初期化は成功しても、実機の耳元操作の通知が届かず改善しませんでした。
効果を確認できない受け付け処理は含めていません。再調査では録音停止後のマイク解放と、
物理操作によるコールバックを別々に確認し、登録成功だけで解決と判断しないでください。
