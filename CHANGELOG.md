# 変更履歴

利用者に関わる変更を、新しい版から記録します。版を上げるコミットでこのファイルも更新します。
過去の詳しい記録は [GitHub Releases](https://github.com/pochang6/nobetsu/releases) を参照してください。

## 1.4.3 — 2026-09-11

- 閉じないチェック項目の見た目を、標準のメニュー項目と同じ（左にチェック、載せると反転）に戻しました。1.4.1 で入れたチェックボックスはやめています。

## 1.4.2 — 2026-09-11

- `build.sh` / `test.sh` が使う SDK を、配置先の macOS の版（26）に固定しました。コマンドラインツールの更新で次の版のベータ SDK が既定になると、SwiftUI のマクロが見つからずビルドが落ちていました。`SDKROOT` を指定すればそちらを優先します。
- メニューバーのアイコンの位置を保存するようにしました（`autosaveName`）。起動時にアイコンの状態をログへ残します。

## 1.4.1 — 2026-09-11

- メニューバーと目印の「…」にあるチェック項目（音で知らせる、入力先から離れたら止める など）を切り替えても、メニューが閉じないようにしました。入ったか外れたかがその場で見えます。
- チェック項目の行はマウスを載せても反転しなくなります。選ぶと閉じる項目（開始に使う ⌘、目印を出す画面）はこれまでどおりです。

## 1.4.0 — 2026-09-07

- 目印の「…」→「目印を出す画面」で、表示するモニターを固定できるようにしました。「この画面に固定」、または接続中の画面の一覧から選べます。
- 固定設定は再起動後も保持します。固定先が未接続の間は入力欄の画面へ自動で移動し、再接続すると固定先へ戻ります。
- 既定はこれまでどおり自動追従です。ドラッグは画面内の位置だけを変え、固定を解除するには「入力欄の画面へ自動で移動」を選びます。
- v1.3.2 の複数画面での位置判定・バックアップ保持の修正も含みます。

## 1.3.2 — 2026-09-07

- ターミナルやエディタが文書全体の大きな入力欄を返す場合、目印が別の画面へ移る問題を修正しました。入力欄とウィンドウの共通部分で画面を選び、共通部分が空ならウィンドウを使います。
- 辞書バックアップの今回分を UUID のフォルダ名で識別するようにしました。5世代を保持し、過去の退避日時が未来でも今回の退避を削除しません。
- iTerm2 の3画面での実測値、上下・左右の配置、座標が欠ける場合、未来日時のバックアップを回帰テストに追加しました。

## 1.3.1 — 2026-09-07

- 辞書の一部が読めなくても、読めた辞書を使います。壊れたリンク・JSON・文字コードなどの問題は、メニューに保存先と対処を表示します。
- 新しく引き継ぐ個人辞書は作業ツリーからコピーし、リンクを作らないようにしました。既存の辞書・リンクは維持します。
- 壊れた引き継ぎ記録や消えたリンク先があっても、残っているデータを退避して更新を続けます。壊れた記録を上書きしたり、古い規則を再取り込みしたりしません。
- 辞書のバックアップを最新5世代に制限しました。新しい退避が完成するまで古い世代を残します。
- 更新失敗後の終了処理で復元が黙って中断される問題を修正しました。終了・削除・復元ができない場合は旧版を退避先に残し、その場所を表示します。
- 更新ロックが残ったとき、他の更新が動いていないことを確かめて解除する手順を表示します。

## 1.3.0 — 2026-09-07

### 個人辞書を守ったまま更新

- 個人辞書は既存ファイル・シンボリックリンクを上書き、移動、削除せず引き継ぎます。
- アプリに同梱する辞書を公開サンプルだけにしました。個人の語彙はローカルファイルとして読みます。
- 旧アプリの中だけに残る辞書もローカルへ回収します。既存の個人辞書へ勝手にマージしません。
- 設置前に辞書をローカルへバックアップします。読めないファイルや壊れたリンクがあれば設置を中止します。
- 引き継いだファイルから規則を消した後、旧同梱辞書の内容を再取り込みして復活させないようにしました。
- 無効な辞書規則をログへ全文出力しないようにしました。個人辞書を公開へ push する古い説明も修正しました。

### 更新と起動・停止

- `./update.sh` で fast-forward 更新、ビルド、設置、再起動、キー監視の起動確認まで行えます。
- コピー・署名検証が済むまで旧アプリを残し、入れ替えや起動確認に失敗したら旧アプリへ戻します。
- `./build.sh --restart` と、設置せずコンパイル・署名だけを行う `--build-only` を追加しました。
- モデル準備中も ESC・⌘・停止メニューで取り消せます。準備・終了処理の重複と古い認識結果の混入を防ぎます。
- 認識エラー時にもマイクを解放し、理由を表示します。音声構成の通知だけで録音を誤停止しません。

### AirPods と目印

- **AirPods の拒否バナー・録音中の Netflix 再生操作の競合は未解決です。**
  ミュートの公開 API は実機で耳元操作を受け取れず、改善を確認できませんでした。
  ESC・⌘ の単独タップ・目印の一時停止ボタンで録音を停止してから、再生操作を試してください。
- 目印がフォーカス中の入力欄のある画面へ追従します。画面内は従来の保存位置を保ち、
  入力欄の移動やスクロールだけでは位置を変えません。座標を取得できない場合は前面ウィンドウの画面などを使います。
- Space・フルスクリーンの切り替え後に表示を更新します。古いフォーカスの問い合わせ結果は破棄します。
- 停止直後に再開した際、前の非表示アニメーションが新しい目印まで消すことを防ぎました。
- 目印の説明で「無音でも録音中」と「一時停止してマイク解放済み」を区別しました。

### 既存ユーザーの更新手順

```bash
git pull --ff-only
./build.sh --restart
```

次回からは `./update.sh` だけで更新できます。個人辞書と設定の再作成は不要です。
辞書の引き継ぎ・バックアップ先は [README](README.md#旧版で育てた辞書の引き継ぎ) に記載しています。

### 検証

辞書引き継ぎ・空ファイルとリンクの保護・状態遷移・目印の座標計算を自動テストしています。
アプリ入れ替えはコピー、署名検証、入れ替え、起動、起動確認の失敗を模擬してロールバックを確認します。
音声の認識精度、AirPods の物理操作、個々のアプリの Space 移動は実機での確認対象です。

## 1.2.2 — 2026-08-26

辞書連携の構成変更を戻し、公開コードを 1.1.0 相当の構成へ揃えました。

## 1.1.0 — 2026-08-25

開始する ⌘ キーの左右と、長押し時間を設定できるようにしました。
停止操作は左右どちらの ⌘ でも使えます。
