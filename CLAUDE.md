# nobetsu — 開発の手引き

日本語話者向けの macOS 音声入力アプリ。⌘ を長押しして喋ると、話した端から、
そのとき使っているアプリに文字が入る。IME を経由せず、時間制限もない。

このファイルは AI エージェントと人間の両方に向けた作業メモです。
個人環境に依存する値は書かないでください（絶対パス、資格情報、個人名など）。

## 環境

- macOS 26 (Tahoe) 以降 / Apple Silicon
- Swift はコマンドラインツール同梱のもので足りる。**Xcode は不要**
- 依存ライブラリなし。すべて OS 標準のフレームワーク

## ビルドと動作確認

```bash
./build.sh                      # ビルドして /Applications/nobetsu.app に設置
pkill -f nobetsu.app            # 動いているものを終了
open /Applications/nobetsu.app  # 起動
```

**変更したら必ずこの3つを通してください。** `/Applications` に置くのは、
システム設定の許可一覧（入力監視・アクセシビリティ）が
そこからしかアプリを選べないためです。

起動してもウィンドウは出ません。メニューバーの波形アイコンだけが目印です。

| アイコン | 状態 |
|---|---|
| 警告の三角 | 許可が足りていない |
| 波形 | 待機中 |
| 波形＋丸 | 認識中 |

## テスト

```bash
./test.sh
```

副作用を持たない部分だけを確かめます（辞書の置換と、打ち込みの差分計算）。
アプリは起動しません。`Sources/App.swift` を含めないのは `@main` が衝突するためです。

**この2つを選んだ理由**は、間違えると利用者の書きかけの文章を壊すからです。
`Injector` の差分計算を誤ると Backspace が他人の文章を削りにいきますし、
辞書の置換が冪等でないと、打ち直すたびに文章が壊れていきます。

新しく純粋な処理を書いたら、ここに足してください。
逆に、マイク・イベントタップ・他アプリへの打ち込みは実機でしか確かめられません。
そちらは `.claude/skills/rebuild/` の手順で動かして見ます。

## ログ

```bash
tail -30 ~/Library/Logs/nobetsu.log
```

**不具合を調べるときは、推測で直す前に必ずログを読んでください。**
このプロジェクトでは、ログを入れずに推測で修正を重ねて半日を失いました。
ログを入れた直後に原因が一発で判明しています。

起動、許可の状態、署名の種類、トリガー、エンジンの遷移、音の再生、
マイクの解放が記録されます。

## 署名（開発時に重要）

macOS の許可は**署名の同一性**に紐づきます。アドホック署名にはそれが無いため、
ビルドのたびに別アプリ扱いになり、許可をやり直すことになります。
入力監視にいたっては、尋ねられることすらなく拒否される場合があります。

キーチェーンアクセスで **`nobetsu`** という名前のコード署名用の自己署名証明書
（固有名のタイプ「自己署名ルート」）を作ってください。`build.sh` が自動で使います。
作ったあと、証明書の「信頼」を開いて**コード署名を「常に信頼」**にする必要があります。
初回のビルド時にキーチェーンのパスワードを聞かれたら「常に許可」を選んでください。

**使うだけなら証明書は不要です。** 開発者向けの手当てです。

## 構成

| ファイル | 役割 |
|---|---|
| `Sources/App.swift` | 全体の配線、メニューバー、許可の要求 |
| `Sources/Dictation.swift` | `SpeechAnalyzer` + `DictationTranscriber` による認識 |
| `Sources/Injector.swift` | 最前面のアプリへ文字を打ち込む。**ここが核心** |
| `Sources/Trigger.swift` | `CGEventTap` によるキーの見張り |
| `Sources/FocusWatcher.swift` | 入力先から離れたかの見張り。離れたら止める |
| `Sources/Phrases.swift` | 辞書（言い換え表）の読み込みと適用 |
| `Tests/main.swift` | 副作用を持たない部分のテスト。`./test.sh` で走る |
| `Sources/Indicator.swift` | 認識中の目印（左上に浮く小さな表示） |
| `Sources/Overlay.swift` | 認識中の文字を流す表示（既定で無効） |
| `Sources/Permissions.swift` | 入力監視とアクセシビリティの判定と要求 |
| `Sources/Sounds.swift` | 開始音と終了音 |
| `Sources/LoginItem.swift` | ログイン時の自動起動 |
| `Sources/Log.swift` | ログ出力 |
| `dictionary.txt` | 共有する辞書。`build.sh` がアプリへ焼き込む |
| `TODO.md` | これからやること。実際に困った順に並べる |
| `PUBLISH.md` | OSS として公開するまでの計画。**公開前に必ず読むこと** |
| `.claude/skills/dictionary/` | 誤変換を見つけて辞書に育てる手順（AI 向け） |
| `spike/` | 採用判断のための計測アプリ（本体とは独立） |

## 設計上の決定（変更する前に読むこと）

**IME を経由しない。**
macOS 標準の音声入力は認識した「よみ」を日本語入力に流し込むため、
ライブ変換が未確定文節を書き換え続けるのと衝突して固まります。
`DictationTranscriber` は漢字かな交じりの確定済みテキストを返すので、
`CGEvent` の Unicode 直接入力を使えば変換工程を丸ごと迂回できます。
**この一点がこのアプリの存在理由です。**

**確定を待たずに打ち込む。**
未確定テキストを先に打ち、認識が書き換わったら共通接頭辞を求めて、
食い違った末尾だけを消して打ち直します。確定を待つと 20 秒以上文字が出ません。

**`SpeechTranscriber` は使わない。**
計測では初回表示に 11.79 秒、確定が最大 8.34 秒遅れました。
喋りながら見る用途には使えません。`DictationTranscriber` を使ってください。

**許可は 入力監視 → アクセシビリティ の順に求める。**
`AXIsProcessTrusted()` を先に呼ぶと入力監視の要求が通らなくなる既知の不具合があります。
また、キーを飲み込めるタップ（`.defaultTap`）は**両方**の許可を要求します。
許可ダイアログを出すのは `CGRequestListenEventAccess` であって、
`CGEventTap` の生成ではありません。生成は黙って `nil` を返すだけです。

**打ち込みの停止はイベントタップの中で同期的に行う。**
コールバックはキーがアプリへ届く「前」に呼ばれます。ここで止めないと、
Enter で送信した直後に、溜まっていた文字が空の入力欄へ流れ込みます。
処理は配列の退避とタイマー停止だけなので、タップを塞ぐ心配はありません。
**ただし重い処理（ダイアログ表示、エンジン起動）は必ず非同期に逃がしてください。**
コールバックが長引くと macOS がタップを無効化し、以後キーが一切効かなくなります。

**開始音は「聞ける状態になってから」鳴らす。停止音は「押した瞬間」に鳴らす。**
音が鳴った瞬間に喋りはじめるのが自然なので、鳴らすのが早すぎると頭が削られます。
以前は無線イヤホンの接続切り替え（0.5秒ほど音が途切れる）に飲まれるのを避けるため
マイクを起こす直前に鳴らしていましたが、**先頭の一文字が入らない**という
別の問題を生みました。いまはマイクが動いてから 0.25 秒置いて鳴らします。
止めるときは逆で、押した瞬間に鳴らします。待たせると無反応な時間が生まれます。

**停止したら必ずマイクを手放す。**
`AVAudioEngine` を止めるだけでは入力ノードがマイクを掴んだままになり、
macOS 標準の音声入力が起動直後に切れます。
tap を外す → 停止 → reset → インスタンス破棄まで揃えてください。

**辞書は「打つ直前」に当てる。**
音声認識は「Clone」を「苦労」と書く。話し言葉としては正しいので、
認識器の側では直りません。未確定テキストは毎回その区間の全文が届くので、
`PhraseBook.apply` は状態を持たない純粋な置換で足ります。
途中で一瞬おかしな形に化けても、次の更新で打ち直されます。
辞書は2枚（アプリ同梱＝git で共有 / 個人＝その Mac だけ）で、後者が勝ちます。
読むのは**喋りはじめる瞬間**なので、書き換えたら次のひと言から効きます。

**Enter は「送信」であると同時に「利用者の操作」でもある。**
イベントタップは `userTookOver()` を先に呼ぶので、`userSubmitted()` に届く時点で
`pending` は既に空になっており、打ち込み済みの内容は `abandonedPrefix` にあります。
これを知らずに `pending` を送信済みと見なすと「何も打っていない」ことになり、
**その区間の全文が、空になった入力欄へ丸ごと打ち直されます。**
送信した文章がそのまま次のメッセージに現れる形で実際に起きました。
Enter を2回叩いたときに短い方で上書きしないことも含めて、
`submittedPrefix` は**縮む方向へ更新しません**。

**送信したら、その文章は二度と触らない。**
認識は後から言い回しを直します。送信したあとにその直しが届くと、
食い違った位置まで消して打ち直そうとしますが、そこはもう画面にありません。
結果、**送信し終えた文章の後半が、丸ごと空の入力欄へ打ち込まれます**
（実際に「最後の100文字が残る」形で起きました）。
送信済みの文章はもう相手の手元にあり、こちらから直す手立てはありません。
`virtualPrefix` は「打ち込み済みと見なしているが、実際には画面に無い文字数」で、
そこより手前が書き直されたら、その区間は追いかけるのをやめます。

**Shift+Enter は送信ではない。**
Enter を送信とみなすと区間を切り、切れ目の確定が届くまで打ち込みを止めます。
これを改行（Shift+Enter）でやると、空行を入れて喋りはじめた瞬間に
数秒なにも入らず「固まった」ように見えます。実際にログで再現しました
（`区間を区切る` が 0.17 秒差で2行並ぶ＝空行を入れた Enter 2連打）。
Shift が付いた Enter は改行として扱い、区間を切りません。
無修飾の Enter は送信のままですが、そのあとも喋り続けていれば
`tryResumeAfterSubmit` が拾い直すので、止まったままにはなりません。

**入力先から離れたら止める（既定）。通知ではなく、見に行く。**
始めたことを忘れたまま別のアプリへ移ると、独り言が同僚宛の入力欄へ流れ込みます。
事故の代償が大きいので、既定は「離れたら止める」に倒しています。

最初は Accessibility の「フォーカスが変わった」通知で作りましたが、**逆に動きました。**
離れても止まらず、戻ってきた瞬間に止まる。通知はアプリによって出たり出なかったりし、
前面に戻る一瞬だけ別の要素を指すため、その瞬間を「離れた」と誤解するためです。
いまは 0.4 秒ごとに**今のフォーカスを見に行き**、
「文字を入れられない場所にいる」が2回続いたときだけ止めます。
アプリの切り替えだけは通知の方が速いので併用しています。

**「見えない」は「離れた」ではありません。**
入力欄を一切教えてくれないアプリがあります（Claude や Slack のような Electron 製が典型）。
そこで「フォーカスが取れない＝離れた」と判断したところ、
**喋りはじめて 0.8 秒で必ず止まる**という、使い物にならないものができました。

Electron/Chromium 製のアプリは、頼まれるまで入力欄の情報を作りません。
見張りはじめに `AXManualAccessibility` を立てて用意してもらいます
（支援技術が使うのと同じ頼み方）。それでも見えないアプリでは、
アプリと画面の切り替えだけを見ます。**一度も見えていないことを理由に止めません。**

**入力欄かどうかは、役割で決めます。「選択範囲を持っているか」では決められません。**
Chromium は入れ物（`AXGroup`）にも選択範囲を持たせるため、
文章の上をクリックしただけで「まだ入力欄にいる」と誤判定しました。
`AXWebArea`（ページそのもの）も入力欄ではありません。
ページに focus が乗っているだけで、そこへ打った文字はどこへも入りません。

**最初に見えた場所は、役割を問わず基準にします。**
利用者はいまそこで喋りはじめたのだから、そこが入力先に決まっています。
見慣れない役割名を返すアプリを、いきなり「入力欄ではない」と切り捨てないためです。

**相手のアプリへの問い合わせを、メインスレッドでやってはいけません。**
Accessibility の問い合わせは同期的で、相手が応答しなければその場で待たされます。
メインスレッドは**キーの見張り（イベントタップ）と同じ**なので、
そこが詰まると `⚠️ イベントタップが無効化された (timeout)` が出て、
**Mac 全体のキー入力が止まったように見えます。**
問い合わせは別のキューへ逃がし、結果だけを持ち帰ってください。
目印の 25Hz タイマーを消し忘れないのも同じ理由です。

自分（目印のメニュー・メニューバー）が前面にいる間は判定しません。
設定を見ようとしただけで止まってしまうためです。
同じアプリの中で別の**入力欄**へ移っただけなら止めず、打ち込み済みの位置だけ捨てます。

**戻ってきたら、カーソルの位置を見る。**
よそを触って入力欄へ戻ると、アプリがカーソルを先頭へ置き直すことがあります。
そこへ打ち込むと、書きかけの文章の**頭に**続きが入ります。
「先頭ちょうど・選択なし・中身は空でない」ときだけ末尾へ送ります。
自分でカーソルを置いた場合は触りません。途中に差し込みたいことはあるからです。

**押していないのに止まるときは、音を変える。**
終了音と同じでは、何が起きたのか分からないまま驚くだけになります。
止まった理由はメニューにも残します。

**目印は「いま作業している画面」に出す。**
`NSScreen.main` は「自分のキーウィンドウがある画面」を返すため、
キーウィンドウを持たないこのアプリでは**常にメインディスプレイ**を指します。
サブモニターで喋っているのに目印だけメインに出ては、誰も気づけません。
マウスのいる画面を使い、覚える位置も**画面の左下からの距離**にしています。
絶対の座標で覚えると、別の画面で喋ったときに画面の外へ出ます。

**目印はフォーカスを奪ってはいけない。**
奪うと打ち込んだ文字が入力先ではなく目印に入り、機能そのものが壊れます。
`nonactivatingPanel` と `canBecomeKey = false` で担保しています。

## よくある調査

| 症状 | まず見るところ |
|---|---|
| ⌘ 長押しに反応しない | ログの `trigger:` 行。許可の状態が出る |
| 文字が入らない | アクセシビリティの許可。ログの `start:` 行 |
| 音が鳴らない | ログの `sound:` 行。無線イヤホンは接続切替で音が途切れる |
| 文字が重複する / 消える | `Injector.swift` の区間管理。`accepting` と `discardCurrentSpan` |
| 送信した文章が入力欄に残る | ログの `injector: 送信済みの部分が書き直された`。`virtualPrefix` |
| 他アプリの音声入力が壊れる | ログの `audio: マイクを解放した` が出ているか |
| 喋っているのに数秒なにも入らない | ログの `engine: 区間を区切る`。送信扱いの Enter が犯人 |
| 勝手に止まる | ログの `focus:` 行。離れたと判断した理由と、相手の役割名が出る |
| キー入力ごと効かなくなる | ログの `イベントタップが無効化された`。メインスレッドが詰まっている |
| 目印の上でカーソルが消える | `Indicator.nudgeCursor`。隠しているのは相手のアプリ |
| 入力欄を離れても止まらない | ログの `focus:` 行の役割名。`AXGroup` を入力欄と誤認していないか |
| 辞書が効かない | ログの `phrases:` 行。読み込んだ件数と読んだファイルが出る |

## 運用

- バージョンは `VERSION` ファイルで管理し、`build.sh` が Info.plist に埋め込みます
- 自分の変更は `main` へ直接コミットします（ブランチも Pull Request も挟みません）
- 外部からの変更はフォークと Pull Request で受け付けます
  （フォークなしに他人がこのリポジトリへブランチを作ることはできません）
